= form_for @contact do |f|
  - if @contact.errors.any?
    #error_explanation      
      %ul
        - @contact.errors.each do |attr, msg|
          %li= msg
  .col-xs-5.margin5 
    .form-group
      = f.label 'فکس:', :class => "control-label"
      = f.text_field :fax, :class => "form-control"
    .form-group
      = f.label 'آدرس', :class => "control-label"
      = f.text_field :address, :class => "form-control"
    .form-group
      = f.submit 'ذخیره', :class => "btn btn-default"

  .col-xs-5.margin5 
    .form-group
      = f.label 'نام شخص:', :class => "control-label"
      = f.text_field :preson_name, :class => "form-control"
    .form-group
      = f.label 'نام شرکت:', :class => "control-label"
      = f.text_field :company_name, :class => "form-control"
    .form-group
      = f.label 'تلفن:', :class => "control-label"
      = f.text_field :phone, :class => "form-control"
  
